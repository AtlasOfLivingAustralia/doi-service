apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ala-doi.fullname" . }}-test-connection"
  labels:
    {{- include "ala-doi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ala-doi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
