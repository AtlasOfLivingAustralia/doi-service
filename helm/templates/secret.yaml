apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
  labels:
    {{- include "ala-doi.labels" . | nindent 4 }}
type: Opaque
data:
  doi-service-config.yml: |
    {{- tpl (.Files.Get "config/doi-service-config.yml") . | b64enc | nindent 4 }}